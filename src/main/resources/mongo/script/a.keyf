function(doc){
	var date = doc._id.getTimestamp();
	var y = date.getFullYear();
	var M = date.getMonth() + 1;
	var d = date.getDate();
	var h = date.getHours();
	if(h % 2 == 1) {
		h = h - 1;
	}
	var key = "" + h + "-" + M + "-" + d + " " + h;
	return {'key':key};
}